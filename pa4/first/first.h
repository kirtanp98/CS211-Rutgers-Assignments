#ifndef first_h
#define first_h

int binaryToDecimal(char* binary);
char* decimalToBinary(int decial, int length);
char* hexToBinary(char* hex);
void enqueue(char* bit);
void enqueuePre(char* bit);
void dequeue();
void dequeuePre();
int search(char* bit);
int searchPre(char* bit);

#endif
